Summary
-------
A JSON parser library written in C

Building/Installation
---------------------
See the INSTALL file for installation instructions.
Building is as simple as `./configure && make && make install`.

